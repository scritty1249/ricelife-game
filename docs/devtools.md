# Development tools
Tools can be downloaded locally, or visiting from Github Pages as a static browser page from the `/tools` directory.

Terrian creator tool, for drawing new maps, can be found at `/tools/terrain-creator`.

Terrain renderer tool, for generating map thumbnails and previews, can be found at `/tools/terrain-renderer`.